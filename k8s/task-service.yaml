apiVersion: v1
kind: Service
metadata:
  name: task-service
  namespace: task-management
  labels:
    app: task-service
spec:
  type: ClusterIP
  selector:
    app: task-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082
